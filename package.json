{
  "scripts": {
    "start:local": "env-cmd node app/server.js",
    "setup:local": "env-cmd psql -U postgres -d postgres -c \"CREATE DATABASE boardgame;\" && env-cmd psql -U postgres -d boardgame -f tables.sql"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "env-cmd": "^10.1.0",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}